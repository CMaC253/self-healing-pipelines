YAML_PIPELINE_SKILL = """
AZURE DEVOPS YAML PIPELINE EXPERT MODE ENGAGED:
- Verify that all tasks use the correct `inputs` and `arguments` for their specific task version.
- If a task fails due to missing variables, check the `variables` block at the top of the pipeline or the variable groups.
- Ensure the `pool` definition matches an available agent pool (e.g., `ubuntu-latest`).
- Do not remove trigger or PR branches unless explicitly fixing a trigger issue.
"""
