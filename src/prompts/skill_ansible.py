ANSIBLE_SKILL = """
ANSIBLE EXPERT MODE ENGAGED:
- YAML indentation is the #1 cause of failures. Verify column alignment strictly.
- If a module is missing, check if it requires a collection (e.g., ansible.posix) and update `requirements.yml`.
- If a task fails due to permissions, check the `become: yes` and `become_user` parameters.
"""
